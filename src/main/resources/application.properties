spring.application.name=gasbackend
springdoc.swagger-ui.path=/swagger
springdoc.api-docs.path=/api-docs
spring.security.user.name=admin
spring.security.user.password=admin
server.servlet.context-path=/api

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=admin
spring.datasource.password=admin
spring.sql.init.platform=h2

# Activer la console H2 pour voir la BDD en m moire
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Flyway configuration (Flyway appliquera automatiquement les migrations)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Secret key for JWT Token
jwt.secret=HOL1IepVDSldhQ4QO7dd887yovzdVNOBNqbFNGE5p/A=

spring.devtools.restart.exclude=io/jsonwebtoken/**
spring.devtools.restart.enabled=false